name = "rdx-weekly-case"
main = "rdx-weekly-case-worker.js"
compatibility_date = "2024-01-01"

# DISABLED — superseded by Supabase edge function `weekly-case-email`
# This worker was sending Mon + Thu at 12pm ET. The Supabase function now handles
# all weekly sends with expanded content (cases + puzzles + modules + CoachDx).
# To re-enable: uncomment the [triggers] block below and run `wrangler deploy --config wrangler-weekly.toml`.

# [triggers]
# crons = [
#   "0 17 * * MON",
#   "0 17 * * THU"
# ]

# Secrets — set via:
#   wrangler secret put RESEND_API_KEY
#   wrangler secret put SUPABASE_ANON_KEY
